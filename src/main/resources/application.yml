spring:
  profiles:
    active: prod2

feign:
  client:
    config:
      default:
        logger-level: DEBUG

logging:
  level:
    com.example: DEBUG
    web: DEBUG
    org.springframework.web: DEBUG